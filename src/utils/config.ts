export const todoDataURL = `https://jsonplaceholder.typicode.com/todos`;

export const querySelector = {
    select: 'select'
}

export const values = {
    dropdown: {
        default: "Default",
        disabled: "No colored todos"
    },
    color: {
        default: "#ADD8E6"
    }
}